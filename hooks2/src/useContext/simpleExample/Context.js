import { createContext } from "react";

// UserContext is created with default value "Hammad"
const UserContext = createContext("Hammad");
// it returns two components Provider and Consumer

export { UserContext };
